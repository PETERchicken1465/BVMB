@model IEnumerable<DatVe.Models.tb_HangGhe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenHangGhe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHangGhe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaHangGhe }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaHangGhe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaHangGhe })
        </td>
    </tr>
}

</table>
